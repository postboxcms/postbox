import React from "react";
import { useDispatch } from "react-redux";

import { setRoute } from "./store/route";
import { history } from "./utils/history";

const init = () => {
    // const dispatch = useDispatch();
    // React.useEffect(() => {
    //     dispatch(setRoute(history.location?.pathname));
    // },[]);
}

export default init;