os:
  - linux
language: php
php:
  - '7.2'
before_script:
  - composer self-update
  - composer install --no-interaction
  - cp .env.testing .env
  - nvm install node
  - npm install npm@latest -g
  - npm install
script:
  - sudo vendor/bin/phpunit