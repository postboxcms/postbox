[Unit]
Description=PostboxCMS SSR Server
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory={{DOCUMENT_ROOT}}
ExecStart=/usr/bin/pm2 start server
Restart=always
Environment=NODE_ENV=production

[Install]
WantedBy=multi-user.target
